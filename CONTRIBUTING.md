# Contributing

This is a no-build React SPA (UMD React + Babel) hosted on Cloudflare Pages.
Training content is stored in content/ as JSON + Markdown.

## Local Dev (refresh-safe routes)

### Default (port 8787)
```bash
python scripts/dev_server.py
